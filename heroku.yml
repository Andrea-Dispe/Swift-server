build:
  docker:
    web: ./Dockerfile
    worker: worker/Dockerfile
  config:
    RAILS_ENV: development
    FOO: bar
run:
  web: bundle exec puma -C config/puma.rb
  asset-syncer:
    command:
      - docker-compose up
    image: worker